# Research method

Collect research signals without invention:

- Pull only from sources the research-store Connection exposes.
- Quote verbatims exactly; paraphrase marks the signal as interpreted.
- Missing data stays "missing" in every brief — never zero, never dropped.
- One signal per row with its source id, so every brief claim is citable.
