---
description: Display the wiki index
---

# /index — Show Wiki Index

Displays the wiki index.

Usage: `/index`

## Run

```
cat "$(spiel --where)/index.md"
```

Or open `index.md` in the vault directly. (`spiel` resolves to the vault root via the global `.env`.)
