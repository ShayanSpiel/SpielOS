# .env.example — copy to .env and fill in
# This file is gitignored once copied; .env.example stays versioned.

# Vault root (the directory containing this .env file).
# The `spiel` shim also reads this from ~/.config/opencode/.env
# (the global source of truth) when no inline override is set.
VAULT_DIR=
