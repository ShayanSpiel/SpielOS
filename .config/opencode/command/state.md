# /state

**State machine:** System — read-only
**Template:** AGENTS.md

## Usage
```
/state              # Show current system state
```

## Output
- Current .wiki-state (state, last change, pending action)
- Current .content-config (posting mode, threshold)
- Summary of last validation results
